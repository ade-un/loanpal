{
    "version": 2,
    "builds": [
      {
        "src": "loan_management/wsgi.py",
        "use": "@vercel/python"
      }
    ],
    "routes": [
      {
        "src": "/(.*)",
        "dest": "loan_management/wsgi.py"
      }
    ],
    "build": {
      "commands": {
        "vercel-build": "python manage.py collectstatic --noinput && python manage.py migrate", 
        "build": "python manage.py collectstatic --noinput && python manage.py migrate"  
      },
      "outputDirectory": "."  
    },
    "installCommand": "pip install -r requirements.txt"  
  }
  